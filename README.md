This contains all my SQL practices. It covered many important types of queries:
    *Basic SELECT statement
    *Complex SELECT statment
        -WHERE
        -ORDER BY
        -LIMIT
        -Aggregate functions and Data Transformations
        -GROUP BY
        -Joining tables
    *Building temporary tables
